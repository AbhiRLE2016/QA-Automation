# As a manager

- **Story id:** `as_a_manager__40a4e28b`
- **Last updated:** 2026-05-19 09:41:31
- **Counts:** 1 feature(s) · 1 test(s)  ·  report ✓

## Folder layout

| Path | Contents |
|---|---|
| `features/`      | Gherkin `.feature` files (one per scenario) |
| `pages/`         | Page Object Model classes |
| `step_defs/`     | pytest-bdd step definitions |
| `tests/`         | pytest-bdd test entrypoints |
| `mcp-selectors/` | Live selectors captured by Playwright MCP |
| `reports/`       | HTML + Allure run artifacts |
| `process_log.txt`| Timestamped subprocess history |

## Feature files

- `features/story_1_download_filtered_employees_csv.feature` — All Employees - Download filtered CSV

## Tests

- `tests/test_story_1_download_filtered_employees_csv.py`

## User story

```
As a manager
I want to open the "All Employees" page, pick a finance department from the filter dropdown (or "All Employees")and click "Download CSV"
So that I can quickly find a specific employee in my org and hand over a clean, filtered list to stakeholders without needing database access.
```

_Auto-generated by `agent_ui.py` — refreshed each phase. Edits will be overwritten._

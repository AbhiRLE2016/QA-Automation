# I open enter Manager ID 499 in the username field and Mngr@1…

- **Story id:** `i_open_enter_manager_id_499_in_the_usern__7ebdb375`
- **Last updated:** 2026-05-20 11:53:03
- **Counts:** 1 feature(s) · 1 test(s)  ·  report ✓

## Folder layout

| Path | Contents |
|---|---|
| `features/`      | Gherkin `.feature` files (one per scenario) |
| `pages/`         | Page Object Model classes |
| `step_defs/`     | pytest-bdd step definitions |
| `tests/`         | pytest-bdd test entrypoints |
| `mcp-selectors/` | Live selectors captured by Playwright MCP |
| `reports/`       | HTML + Allure run artifacts |
| `process_log.txt`| Timestamped subprocess history |

## Feature files

- `features/story_1_verify_all_employees_against_csv.feature` — Verify All Employees table against the CSV data source

## Tests

- `tests/test_story_1_verify_all_employees_against_csv.py`

## User story

```
I open http://localhost:5173/, enter Manager ID 499 in the username field and Mngr@101Pass! in the password field, and click the Login button. On the dashboard I click the "View All Employees" button to reach /all-employees/499. I select "All Employees" in the filter dropdown, leave the search box empty, and then read the names in the table and tick each one off against the CSV. To move through the list I click the ">" (next page) button in the pagination footer until I reach the last page, using "<" (previous page) if I need to go back. If every CSV name appears on the UI then the data matches; if any name from the CSV is not found then the UI is missing that employee.
```

_Auto-generated by `agent_ui.py` — refreshed each phase. Edits will be overwritten._
